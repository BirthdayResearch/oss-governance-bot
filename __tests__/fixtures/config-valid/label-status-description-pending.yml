version: v1

issue:
  labels:
    - prefix: triage
      list: [ "accepted" ]
      needs:
        status:
          context: Status Context
          description:
            pending: Pending description
